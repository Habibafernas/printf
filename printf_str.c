#include "main.h"
/**
 * _strlen - returns the lenght of the string
 * @s : string to calculate its length
 * Return : length of the string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		;
	}
	return (i);
}
